import 'package:flutter/material.dart';

class ChatScreen extends StatelessWidget {
  const ChatScreen(userId, { Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}